[project]
name = "cognee-community-graph-adapter-typedb"
version = "0.1.0"
description = "TypeDB graph database adapter for cognee"
authors = [
    {name = "TypeDB"},
]
dependencies = [
    "cognee==1.4.2",
    "typedb-driver>=3.12.0,<4",
    # Transitive via cognee; pinned to the version cognee 1.4.2 locks (litellm
    # 1.96.x published cp310-only wheels, uninstallable on the CI's Python 3.11).
    "litellm==1.89.2",
    # Transitive via cognee; ladybug 0.18.x ships macosx_15_0-only wheels,
    # forcing a cmake source build on macOS 13/14 dev machines.
    "ladybug<0.18",
    "starlette>=0.48.0",
    "instructor>=1.11"
]
readme = "README.md"
requires-python = ">=3.10,<3.14"

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]
# The Anthropic + local-embeddings setup in .env.example (examples, e2e tier).
llm = [
    "anthropic>=0.40",
    "fastembed>=0.4",
    "onnxruntime>=1.17",
]
# Comparative benchmarks against cognee's built-in Neo4j adapter (benchmarks/).
bench = [
    "neo4j>=5.28.0,<6",  # cognee 1.4.2's own neo4j extra pins the same floor
]

[project.urls]
Homepage = "https://github.com/topoteretes/cognee"
Repository = "https://github.com/topoteretes/cognee-community"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cognee_community_graph_adapter_typedb"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "B", "UP", "C4", "PGH", "SIM", "RUF"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
# RUF012: pydantic model fields on DataPoint subclasses are not class vars.
"tests/**" = ["RUF012"]
# Benchmark scripts: pydantic fields, long print lines, small lambdas.
"benchmarks/**" = ["RUF012", "E501", "E731"]
# Dataset handler module is CamelCase by cognee convention (Neo4jDatasetDatabaseHandler.py etc.).
"cognee_community_graph_adapter_typedb/TypeDBDatasetDatabaseHandler.py" = ["N999"]
